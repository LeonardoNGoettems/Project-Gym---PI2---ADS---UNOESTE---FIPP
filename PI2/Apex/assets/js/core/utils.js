(function(){'use strict';window.Gym=window.Gym||{};Gym.Utils={id(prefix='id'){return prefix+'_'+Date.now().toString(36)+'_'+Math.random().toString(36).slice(2,8)},today(){return new Date().toISOString().slice(0,10)},addDays(date,days){const d=new Date(date+'T12:00:00');d.setDate(d.getDate()+Number(days));return d.toISOString().slice(0,10)},esc(value){const d=document.createElement('div');d.textContent=value??'';return d.innerHTML},money(value){const s=Gym.state?.settings||{};return new Intl.NumberFormat('en-US',{style:'currency',currency:s.currency||'USD'}).format(Number(value)||0)},date(value){if(!value)return '—';return new Intl.DateTimeFormat('en-US',{year:'numeric',month:'short',day:'numeric'}).format(new Date(value+'T12:00:00'))},datetime(value){return new Intl.DateTimeFormat('en-US',{dateStyle:'medium',timeStyle:'short'}).format(new Date(value))},name(e){return e?`${e.firstName||''} ${e.lastName||''}`.trim():'Unknown'},norm(v){return String(v??'').trim().toLowerCase().replace(/\s+/g,' ')},statusClass(v){return 'badge badge-'+String(v).toLowerCase().replace(/\s+/g,'-')},overlap(aStart,aEnd,bStart,bEnd){return aStart<bEnd&&bStart<aEnd}}})();
